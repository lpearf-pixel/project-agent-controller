# v0.2 Controlled Runner Implementation Plan

1. Add strict task-template models and configuration contracts, including path and secret
   rejection tests.
2. Add task/circuit persistence with idempotent request creation and bounded attempt
   records.
3. Implement Git HEAD archive isolation, safe extraction, scrubbed environment, fixed
   argv execution, process-group timeout, and bounded/redacted output.
4. Implement retries, controller-state gate, per-project circuit breaker, and a local CLI
   command.
5. Add the Community Selection verification-only template and operator documentation.
6. Add a cross-platform real-execution preflight to the public release workflow.
7. Run formatting, strict typing, the full test suite, temporary Git/task preflights, and
   the existing Docker/GitHub gates before publishing a draft PR.
